volumes:
  tesla_ble:
    external: true

services:   
  tesla_ble:
    container_name: tesla_ble
    image: "iainbullock/tesla_ble:latest"
 
    environment:
      - TZ='Europe/London'
      
    stdin_open: true
    tty: true         

    volumes:
      - tesla_ble:/data
        
    network_mode: bridge
    ports:
      - 14422:22

    restart: no
    #restart: unless-stopped  
